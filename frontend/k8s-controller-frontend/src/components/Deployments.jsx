import React, { useEffect, useState } from 'react';

const Deployments = () => {
    //const [pods, setPods] = useState([]);


    return (
        <div className='component-deployments-container'>
            <h1>Deployments</h1>
            {/* <ul>
                {pods.map(pod => (
                    <li key={pod.id}>{pod.name}</li>
                ))}
            </ul> */}
        </div>
    );
};

export default Deployments;
