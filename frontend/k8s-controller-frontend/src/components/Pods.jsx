import React, { useEffect, useState } from 'react';

const Pods = () => {
    //const [pods, setPods] = useState([]);


    return (
        <div className='component-pods-container'>
            <h1>Pods</h1>
            {/* <ul>
                {pods.map(pod => (
                    <li key={pod.id}>{pod.name}</li>
                ))}
            </ul> */}
        </div>
    );
};

export default Pods;
